package ${package.Service};

import ${package.VO}.${vo};
import ${superServiceClassPackage};

/**
 * <p>
 * 服务类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${table.serviceName} extends ${superServiceClass}<${vo}> {
	
}
